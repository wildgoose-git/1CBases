
Процедура ОбработкаЗаполнения(ДанныеЗаполнения, СтандартнаяОбработка)

	Если ТипЗнч(ДанныеЗаполнения) = Тип("СправочникСсылка.ЗадачаПроекта") Тогда
		Владелец = ДанныеЗаполнения.Владелец;
	ИначеЕсли ТипЗнч(ДанныеЗаполнения) = Тип("СправочникСсылка.Проекты") Тогда
		Владелец = ДанныеЗаполнения.Ссылка;
	КонецЕсли;
	
	Статус = Перечисления.СтатусыЗадач.ВПланировании;
	

КонецПроцедуры
